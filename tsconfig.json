{
  "//": "Type-check da 1ª leva do IPC (sem emitir, sem .ts). Escopo: o par CJS preload↔contract — pega divergência entre a implementação do preload e o IpcApi. api-service.js (ESM) usa `// @ts-check` no editor (módulo diferente; fora deste tsc p/ evitar clash CJS/ESM). Rodar: `npm run typecheck`.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "skipLibCheck": true,
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "esModuleInterop": true,
    "types": ["node"],
    "lib": ["ES2022", "DOM"]
  },
  "include": [
    "preload.js",
    "src/ipc/contract.js"
  ]
}
